---
- name: Install exa
  ansible.builtin.package:
    name: exa
    state: present
  become: "{{ package_become }}"

- name: Copy fish configuration
  ansible.builtin.copy:
    src: fish/
    dest: ~/.config/fish/
